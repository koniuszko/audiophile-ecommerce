export enum UserRole {
    customer = "CUSTOMER",
    owner = "OWNER"
}

export enum PaymentMethod {
    e_money = "CARD",
    cash_on_delivery = "CASH"
}

export enum PaidCompletion {
    no = 0,
    yes = 1
}